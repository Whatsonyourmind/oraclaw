---
phase: 04-developer-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mission-control/apps/api/src/plugins/swagger.ts
  - mission-control/apps/api/src/plugins/swagger.test.ts
  - mission-control/apps/api/package.json
autonomous: false
requirements: [DX-01]

must_haves:
  truths:
    - "Scalar interactive playground is served at /docs"
    - "OpenAPI spec reports version 3.1.0"
    - "API info shows OraClaw Decision Intelligence API with version 2.3.0"
    - "@fastify/swagger-ui is uninstalled and no longer imported"
  artifacts:
    - path: "mission-control/apps/api/src/plugins/swagger.ts"
      provides: "OpenAPI 3.1 + Scalar registration"
      exports: ["registerSwagger"]
      contains: "3.1.0"
    - path: "mission-control/apps/api/src/plugins/swagger.test.ts"
      provides: "Tests for Scalar and OpenAPI version"
      min_lines: 30
  key_links:
    - from: "mission-control/apps/api/src/plugins/swagger.ts"
      to: "@scalar/fastify-api-reference"
      via: "dynamic import and register"
      pattern: "@scalar/fastify-api-reference"
    - from: "mission-control/apps/api/src/plugins/swagger.ts"
      to: "@fastify/swagger"
      via: "register with openapi 3.1.0"
      pattern: "openapi.*3\\.1\\.0"
---

<objective>
Replace Swagger UI with Scalar interactive playground and upgrade OpenAPI spec to 3.1.0.

Purpose: DX-01 gives developers a modern, interactive API playground at /docs with built-in try-it-out functionality, replacing the dated Swagger UI. The OpenAPI 3.1.0 upgrade aligns with current JSON Schema 2020-12 standards.

Output: Updated swagger.ts plugin, uninstalled @fastify/swagger-ui, installed @scalar/fastify-api-reference, tests, visual verification.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-developer-experience/04-RESEARCH.md

@mission-control/apps/api/src/plugins/swagger.ts
@mission-control/apps/api/src/index.ts

<interfaces>
<!-- Current swagger.ts exports a single function used by index.ts -->

From src/plugins/swagger.ts:
```typescript
export async function registerSwagger(fastify: FastifyInstance): Promise<void> {
  // Currently registers @fastify/swagger with OpenAPI 3.0.3
  // Currently registers @fastify/swagger-ui at /docs
}
```

From src/index.ts (line 75):
```typescript
registerSwagger(server);
// This call does NOT change -- only the internal implementation of registerSwagger changes
```

From package.json (current dependencies to swap):
```json
"@fastify/swagger": "^9.7.0",       // KEEP -- used for spec generation
"@fastify/swagger-ui": "^5.2.5",    // REMOVE -- replaced by Scalar
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace Swagger UI with Scalar and upgrade to OpenAPI 3.1</name>
  <files>
    mission-control/apps/api/src/plugins/swagger.ts,
    mission-control/apps/api/src/plugins/swagger.test.ts,
    mission-control/apps/api/package.json
  </files>
  <behavior>
    - Test 1: /docs route exists and returns 200 (Scalar playground is served)
    - Test 2: OpenAPI JSON spec at the swagger route reports openapi version "3.1.0"
    - Test 3: OpenAPI spec info.title is "OraClaw Decision Intelligence API"
    - Test 4: OpenAPI spec info.version is "2.3.0"
    - Test 5: OpenAPI spec components.securitySchemes includes apiKey scheme
    - Test 6: OpenAPI spec has tags for algorithm categories (Optimize, Simulate, Solve, etc.)
  </behavior>
  <action>
    1. Install Scalar and uninstall Swagger UI:
       ```bash
       cd mission-control && npm install @scalar/fastify-api-reference && npm uninstall @fastify/swagger-ui
       ```
       Run from monorepo root so that the workspace resolution works correctly.

    2. Rewrite `src/plugins/swagger.ts` completely:
       - Remove the `@fastify/swagger-ui` import entirely
       - Keep `@fastify/swagger` import (it generates the spec)
       - Change `openapi: '3.0.3'` to `openapi: '3.1.0'`
       - Update `info.title` to `'OraClaw Decision Intelligence API'`
       - Update `info.description` to describe OraClaw as a decision intelligence API with OODA loop, mentioning 19 algorithms and <25ms performance
       - Update `info.version` to `'2.3.0'`
       - Add servers: localhost:3001 (dev), https://web-olive-one-89.vercel.app (production)
       - Update tags to match current API categories: Optimize, Simulate, Solve, Analyze, Predict, Detect, Score, Plan, Billing, Health
       - Change securitySchemes from bearerAuth/JWT to apiKey scheme: `{ type: 'apiKey', name: 'Authorization', in: 'header', description: 'Bearer <your-api-key> from Unkey' }`
       - Remove the old security: [{ bearerAuth: [] }] global security
       - Remove the old component schemas (APIResponse, AuthTokens, User, Signal, Decision, ExecutionPlan, OracleState) -- these are outdated from the pre-v2 era and route schemas should define their own
       - Register Scalar: `await fastify.register(import('@scalar/fastify-api-reference'), { routePrefix: '/docs' })`
       - If the dynamic import causes TypeScript issues, use: `const scalar = await import('@scalar/fastify-api-reference'); await fastify.register(scalar.default, { routePrefix: '/docs' })`
       - Remove the try/catch wrapper -- let registration errors propagate to Fastify's error handling
       - Remove the console.log (Fastify logger handles startup messages)

    3. Create `src/plugins/swagger.test.ts`:
       - Use Fastify inject pattern: create minimal Fastify app, register the swagger plugin via registerSwagger(), add a dummy GET /api/v1/test route (so the spec has at least one route), call app.ready()
       - Test /docs returns 200 (Scalar serves HTML)
       - Test the OpenAPI JSON spec endpoint: after app.ready(), use `app.swagger()` to get the spec object and assert version, title, tags, security schemes
       - If `app.swagger()` is not available, inject GET to the swagger JSON route (typically /docs/json or the route registered by @fastify/swagger -- check by inspecting registered routes)

    IMPORTANT: The existing `registerSwagger(server)` call in index.ts does NOT need to change. The function signature is identical.
  </action>
  <verify>
    <automated>cd mission-control/apps/api && npx vitest run src/plugins/swagger.test.ts -x</automated>
  </verify>
  <done>
    - @fastify/swagger-ui uninstalled from package.json
    - @scalar/fastify-api-reference installed in package.json
    - swagger.ts registers Scalar at /docs with OpenAPI 3.1.0
    - OpenAPI spec has updated title, version, tags, and security scheme
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Scalar playground and llms.txt</name>
  <files>none</files>
  <action>
    Human verifies Scalar UI renders correctly in browser and llms.txt returns expected content.
    Start the dev server and visit http://localhost:3001/docs and http://localhost:3001/llms.txt.
  </action>
  <verify>Human visual inspection -- Scalar loads, llms.txt returns plain text</verify>
  <done>User types "approved" confirming Scalar playground and llms.txt render correctly</done>
  <what-built>Scalar interactive API playground replacing Swagger UI at /docs, serving OpenAPI 3.1.0 spec with updated OraClaw branding and algorithm category tags.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd mission-control && npm run dev:api`
    2. Visit http://localhost:3001/docs in your browser
    3. Verify:
       - Scalar UI loads (modern dark/light theme, not old Swagger UI green)
       - Title shows "OraClaw Decision Intelligence API"
       - API version shows "2.3.0"
       - Tags visible: Optimize, Simulate, Solve, Analyze, Predict, Detect, Score, Plan, Billing, Health
       - Try-it-out functionality works (click any endpoint, click "Send")
       - Authentication section shows apiKey scheme (not JWT bearer)
    4. Visit http://localhost:3001/llms.txt
    5. Verify: Plain text response with "# OraClaw" heading and endpoint listing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Run the full API test suite to confirm no regressions from the swagger plugin change:

```bash
cd mission-control/apps/api && npx vitest run
```

Verify the TypeScript compilation still passes:

```bash
cd mission-control/apps/api && npx tsc --noEmit
```
</verification>

<success_criteria>
1. Scalar interactive playground serves at /docs with try-it-out functionality
2. OpenAPI spec reports version 3.1.0 with OraClaw branding
3. @fastify/swagger-ui is fully removed (not in package.json, no imports)
4. Full API test suite passes with zero regressions
5. Visual confirmation that Scalar UI renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-developer-experience/04-02-SUMMARY.md`
</output>
