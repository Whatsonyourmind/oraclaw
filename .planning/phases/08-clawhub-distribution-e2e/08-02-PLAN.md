---
phase: 08-clawhub-distribution-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - mission-control/apps/api/src/routes/oracle/e2e-billing.test.ts
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "E2E test verifies free-tier flow: unauthenticated call -> algorithm result -> no Stripe meter event"
    - "E2E test verifies paid-tier flow: API key call -> algorithm result -> Stripe meter event emitted"
    - "E2E test verifies x402 flow: USDC payment header -> algorithm result -> settlement triggered"
    - "E2E test verifies batch flow: batch call -> 50% metered discount"
    - "All 17 algorithm endpoints return valid responses via Fastify inject"
  artifacts:
    - path: "mission-control/apps/api/src/routes/oracle/e2e-billing.test.ts"
      provides: "E2E billing verification test suite"
      contains: "free-tier"
---

<objective>
Create comprehensive E2E billing verification tests covering all three payment paths and all algorithm endpoints.

Purpose: INFRA-03 requires end-to-end verification that free -> paid -> x402 flows all work correctly with proper metering.

Output: A test file that verifies every billing path and all 17 endpoints produce valid results.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E billing verification test suite</name>
  <files>
    mission-control/apps/api/src/routes/oracle/e2e-billing.test.ts
  </files>
  <action>
Create comprehensive test file that uses Fastify inject to verify:
1. All 17 algorithm endpoints return 200 with valid data
2. Free-tier: no auth header -> result returned -> no meter event
3. Stripe-tier: with API key -> result returned -> meter event created
4. x402-tier: with payment header -> result returned -> settlement called
5. Batch: batch call -> correct metering at 50% rate
  </action>
  <verify>
    <automated>cd mission-control && npx vitest run apps/api/src/routes/oracle/e2e-billing.test.ts</automated>
  </verify>
  <done>E2E billing verification test suite passes covering free, paid, x402, and batch billing flows.</done>
</task>

</tasks>
