---
phase: 08-clawhub-distribution-e2e
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - mission-control/scripts/demo-api.sh
  - mission-control/.env.example
autonomous: true
requirements:
  - DIST-03
  - INFRA-03

must_haves:
  truths:
    - "Demo script contains curl commands for all 17 endpoints with realistic payloads"
    - ".env.example documents all required environment variables"
  artifacts:
    - path: "mission-control/scripts/demo-api.sh"
      provides: "Runnable demo script with curl examples for all endpoints"
      contains: "curl"
    - path: "mission-control/.env.example"
      provides: "Environment variable template for deployment"
      contains: "STRIPE_SECRET_KEY"
---

<objective>
Create demo infrastructure and environment documentation for deployment readiness.

Purpose: Enable developers to quickly test all endpoints and deploy OraClaw with correct environment configuration.

Output: A runnable demo script with curl examples and a complete .env.example template.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create demo script and environment template</name>
  <files>
    mission-control/scripts/demo-api.sh,
    mission-control/.env.example
  </files>
  <action>
1. Create `scripts/demo-api.sh` with curl commands for all 17 endpoints
2. Create `.env.example` documenting all required environment variables
  </action>
  <verify>
    <automated>cd mission-control && test -f scripts/demo-api.sh && test -f .env.example && echo "Demo and env template created"</automated>
  </verify>
  <done>Demo script and .env.example created for deployment readiness.</done>
</task>

</tasks>
