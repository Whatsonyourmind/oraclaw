---
phase: 01-auth-and-access-control
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - mission-control/apps/api/src/test-utils/mock-unkey.ts
  - mission-control/apps/api/src/middleware/auth.test.ts
  - mission-control/apps/api/src/services/unkey.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Tests prove a valid API key returns 200 with algorithm result"
    - "Tests prove an invalid key returns 401"
    - "Tests prove a rate-limited key returns 429 with X-RateLimit-Reset header"
    - "Tests prove a disabled key returns 403"
    - "Tests prove no Authorization header returns free tier (no Unkey call)"
    - "Tests prove Unkey SDK errors return 503"
    - "Tests prove X-RateLimit-Remaining, X-RateLimit-Limit, X-RateLimit-Reset appear on success responses"
    - "Tests prove key rotation returns a new key while old key has a grace period"
    - "Tests prove key revocation deletes the key"
    - "All existing 945+ tests still pass (no regression)"
  artifacts:
    - path: "mission-control/apps/api/src/test-utils/mock-unkey.ts"
      provides: "Shared Unkey SDK mock returning configurable verify/create/reroll/delete responses"
      exports: ["createMockUnkey", "mockVerifyValid", "mockVerifyInvalid", "mockVerifyRateLimited"]
    - path: "mission-control/apps/api/src/middleware/auth.test.ts"
      provides: "Tests for auth middleware preHandler + onSend hooks"
      contains: "describe.*auth"
    - path: "mission-control/apps/api/src/services/unkey.test.ts"
      provides: "Tests for key creation, rotation, revocation"
      contains: "describe.*unkey"
  key_links:
    - from: "mission-control/apps/api/src/middleware/auth.test.ts"
      to: "mission-control/apps/api/src/middleware/auth.ts"
      via: "imports createAuthMiddleware, tests preHandler behavior"
      pattern: "import.*createAuthMiddleware.*from.*auth"
    - from: "mission-control/apps/api/src/services/unkey.test.ts"
      to: "mission-control/apps/api/src/services/unkey.ts"
      via: "imports key management functions, mocks Unkey SDK"
      pattern: "import.*createApiKey.*from.*unkey"
    - from: "mission-control/apps/api/src/test-utils/mock-unkey.ts"
      to: "@unkey/api"
      via: "creates mock Unkey class matching SDK interface"
      pattern: "vi\\.fn|mock.*verifyKey"
---

<objective>
Create comprehensive test coverage for Unkey auth middleware and key management, plus a shared mock utility, verifying all four AUTH requirements (AUTH-01 through AUTH-04) with automated tests.

Purpose: The auth middleware and key management service created in Plan 01 need test coverage to prove they work correctly before downstream phases (billing, x402) build on top of them. Tests use mocked Unkey SDK to avoid hitting real Unkey API.

Output: Three test files -- shared Unkey mock utility, auth middleware tests (preHandler/onSend behavior), and key management service tests (create/rotate/revoke). All existing 945+ tests continue to pass.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-access-control/01-RESEARCH.md
@.planning/phases/01-auth-and-access-control/01-01-SUMMARY.md

@mission-control/apps/api/src/services/unkey.ts
@mission-control/apps/api/src/middleware/auth.ts
@mission-control/apps/api/vitest.config.ts

<interfaces>
<!-- From Plan 01 outputs (auth middleware + unkey service) -->
From mission-control/apps/api/src/services/unkey.ts:
```typescript
export const unkey: Unkey;
export const TIER_RATE_LIMITS: Record<string, { limit: number; duration: number }>;
export async function createApiKey(tier: string, stripeCustomerId: string, userEmail: string): Promise<{ key: string; keyId: string }>;
export async function rotateApiKey(keyId: string, expirationMs?: number): Promise<{ key: string }>;
export async function revokeApiKey(keyId: string): Promise<void>;
```

From mission-control/apps/api/src/middleware/auth.ts:
```typescript
// Module augmentation adds to FastifyRequest:
//   tier: string, keyId?: string, stripeCustomerId?: string
//   rateLimitRemaining?: number, rateLimitLimit?: number, rateLimitReset?: number
//   billingPath: 'stripe' | 'free'

export function createAuthMiddleware(unkeyClient: Unkey): (request: FastifyRequest, reply: FastifyReply) => Promise<void>;
export const rateLimitHeadersHook: (request: FastifyRequest, reply: FastifyReply, payload: any) => Promise<any>;
```

From @unkey/api v2.3.2 (verified response structure):
```typescript
// verifyKey response:
// { data: { valid, code, meta, keyId, ratelimits: [{ remaining, limit, reset, exceeded }] }, error: null }
// OR { data: null, error: { message, code } }

// createKey response:
// { data: { key, keyId }, error: null }

// rerollKey response:
// { data: { key }, error: null }

// deleteKey response:
// { data: {}, error: null }
```

Existing test pattern (from oracle.integration.test.ts):
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify, { FastifyInstance } from 'fastify';
// Uses app.inject() for HTTP testing
// Uses describe/it/expect from vitest globals
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared Unkey mock utility and auth middleware tests</name>
  <files>
    mission-control/apps/api/src/test-utils/mock-unkey.ts,
    mission-control/apps/api/src/middleware/auth.test.ts
  </files>
  <behavior>
    AUTH-01 (key verification):
    - Test: Valid key -> request.tier set from meta, request.keyId set, billingPath='stripe', handler called
    - Test: Invalid key (NOT_FOUND) -> 401 response with RFC 9457 body
    - Test: No Authorization header -> request.tier='free', billingPath='free', Unkey NOT called

    AUTH-03 (rate limiting):
    - Test: Rate-limited key (code=RATE_LIMITED) -> 429 response
    - Test: Disabled key (code=DISABLED) -> 403 response
    - Test: Expired key (code=EXPIRED) -> 401 response

    AUTH-04 (rate limit headers):
    - Test: Valid key with ratelimits -> X-RateLimit-Remaining, X-RateLimit-Limit, X-RateLimit-Reset in response headers
    - Test: Rate-limited key -> headers still present on 429 response
    - Test: Free tier (no key) -> no rate limit headers (Unkey not called)

    Error handling:
    - Test: Unkey SDK error (network failure) -> 503 response, NOT 401
  </behavior>
  <action>
**Create `mission-control/apps/api/src/test-utils/mock-unkey.ts`:**

A shared mock factory for the Unkey SDK class. Must match the @unkey/api v2.3.2 interface:

1. Export `createMockUnkey()` that returns an object mimicking the `Unkey` class with `keys` property containing mock functions for `verifyKey`, `createKey`, `rerollKey`, `deleteKey` -- all using `vi.fn()`.
2. Export preset response factories:
   - `mockVerifyValid(overrides?)` -- returns `{ data: { valid: true, code: 'VALID', keyId: 'key_test', meta: { tier: 'starter', stripeCustomerId: 'cus_test' }, ratelimits: [{ remaining: 99, limit: 1667, reset: Date.now() + 86400000, exceeded: false, name: 'api_calls', autoApply: true }] }, error: null }`
   - `mockVerifyInvalid(code)` -- returns `{ data: { valid: false, code, ratelimits: [] }, error: null }`
   - `mockVerifyRateLimited()` -- returns `{ data: { valid: false, code: 'RATE_LIMITED', ratelimits: [{ remaining: 0, limit: 1667, reset: Date.now() + 3600000, exceeded: true }] }, error: null }`
   - `mockVerifySdkError()` -- returns `{ data: null, error: { message: 'Network error', code: 'INTERNAL_SERVER_ERROR' } }`
   - `mockCreateKeySuccess()` -- returns `{ data: { key: 'ok_live_test123', keyId: 'key_new' }, error: null }`
   - `mockRerollKeySuccess()` -- returns `{ data: { key: 'ok_live_rotated456' }, error: null }`

**Create `mission-control/apps/api/src/middleware/auth.test.ts`:**

Test the `createAuthMiddleware` preHandler and `rateLimitHeadersHook` onSend:

1. Set up a minimal Fastify instance in `beforeAll` with a test route (GET /test) that returns `{ tier: request.tier, billingPath: request.billingPath }`.
2. Register the auth middleware as preHandler and rateLimitHeadersHook as onSend on this test app.
3. Use `createMockUnkey()` and configure its `verifyKey` mock per test.
4. Write the following test cases:

**describe('Auth Middleware - preHandler')**
- `it('returns free tier when no Authorization header')` -- inject GET /test with no auth header. Expect 200, body.tier='free', body.billingPath='free'. Verify `verifyKey` was NOT called.
- `it('returns 200 with tier from valid key')` -- mock verifyKey to return valid response with tier='growth'. Inject with `Authorization: Bearer ok_live_xxx`. Expect 200, body.tier='growth', body.billingPath='stripe'.
- `it('returns 401 for NOT_FOUND key')` -- mock verifyKey to return invalid NOT_FOUND. Inject with bearer. Expect 401, body.type contains 'not-found'.
- `it('returns 429 for RATE_LIMITED key')` -- mock verifyKey to return RATE_LIMITED. Expect 429.
- `it('returns 403 for DISABLED key')` -- mock verifyKey to return DISABLED. Expect 403.
- `it('returns 401 for EXPIRED key')` -- mock verifyKey to return EXPIRED. Expect 401.
- `it('returns 503 on Unkey SDK error')` -- mock verifyKey to return SDK error. Expect 503, body.title contains 'unavailable'.

**describe('Rate Limit Headers - onSend')**
- `it('includes X-RateLimit-* headers on valid key response')` -- mock valid key with ratelimits. Inject with bearer. Check response headers: X-RateLimit-Remaining, X-RateLimit-Limit, X-RateLimit-Reset all present.
- `it('includes X-RateLimit-* headers on 429 response')` -- mock rate-limited. Inject with bearer. Expect 429 AND headers present.
- `it('omits X-RateLimit-* headers for free tier')` -- inject without bearer. Expect headers NOT present.

Use `app.inject()` pattern consistent with existing test files. Use `vi.fn()` and `vi.mocked()` for mocking.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/Handler/mission-control/apps/api" && npx vitest run src/middleware/auth.test.ts -x --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - mock-unkey.ts provides reusable mock factory with preset responses for all Unkey operations
    - auth.test.ts has 10+ test cases covering AUTH-01, AUTH-03, AUTH-04
    - All tests pass (valid key -> 200, invalid -> 401, rate limited -> 429, disabled -> 403, SDK error -> 503)
    - Rate limit headers verified present on success and 429 responses, absent on free tier
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create key management tests and run full regression suite</name>
  <files>
    mission-control/apps/api/src/services/unkey.test.ts
  </files>
  <behavior>
    AUTH-01 (key creation):
    - Test: createApiKey calls unkey.keys.createKey with correct apiId, prefix, meta, ratelimits with autoApply:true
    - Test: createApiKey returns { key, keyId } on success
    - Test: createApiKey throws on SDK error

    AUTH-02 (rotation + revocation):
    - Test: rotateApiKey calls rerollKey with keyId and expiration (default 1 hour)
    - Test: rotateApiKey returns new key string
    - Test: rotateApiKey with custom expiration passes it through
    - Test: revokeApiKey calls deleteKey with keyId
    - Test: revokeApiKey throws on error

    Tier config:
    - Test: TIER_RATE_LIMITS has entries for starter, growth, scale, enterprise
    - Test: createApiKey uses correct tier limits from TIER_RATE_LIMITS
  </behavior>
  <action>
**Create `mission-control/apps/api/src/services/unkey.test.ts`:**

Test the key management functions exported from `unkey.ts`:

1. Mock the `@unkey/api` module using `vi.mock('@unkey/api')` to intercept the Unkey constructor and control what `keys.createKey`, `keys.rerollKey`, `keys.deleteKey` return.
2. Import `createApiKey`, `rotateApiKey`, `revokeApiKey`, `TIER_RATE_LIMITS` from the service.
3. Use the mock-unkey.ts preset factories where applicable.

**describe('TIER_RATE_LIMITS config')**
- `it('has all expected tiers')` -- verify keys: starter, growth, scale, enterprise.
- `it('has correct daily limits')` -- starter=1667, growth=16667, scale=166667, enterprise=1000000. All durations=86400000.

**describe('createApiKey')**
- `it('calls createKey with correct parameters')` -- call `createApiKey('starter', 'cus_123', 'user@test.com')`. Verify `keys.createKey` was called with `apiId` from env, `prefix: 'ok_live'`, `meta: { tier: 'starter', stripeCustomerId: 'cus_123' }`, `ratelimits` containing `autoApply: true` and starter limits.
- `it('returns key and keyId on success')` -- mock createKey to return success. Verify return has `key` and `keyId`.
- `it('throws on SDK error')` -- mock createKey to return error. Verify function throws.
- `it('defaults to starter limits for unknown tier')` -- call with tier='unknown'. Verify it falls back to starter limits.

**describe('rotateApiKey')**
- `it('calls rerollKey with keyId and default 1-hour expiration')` -- call `rotateApiKey('key_abc')`. Verify `keys.rerollKey` called with `{ keyId: 'key_abc', expiration: 3600000 }`.
- `it('accepts custom expiration')` -- call `rotateApiKey('key_abc', 0)`. Verify expiration=0 passed (immediate revocation).
- `it('returns new key on success')` -- mock success. Verify return has new `key`.
- `it('throws on error')` -- mock error. Verify throws.

**describe('revokeApiKey')**
- `it('calls deleteKey with keyId')` -- call `revokeApiKey('key_abc')`. Verify `keys.deleteKey` called with `{ keyId: 'key_abc' }`.
- `it('throws on error')` -- mock error. Verify throws.

**After all new tests pass, run the full regression suite:**
```bash
cd mission-control && npm run test
```
Verify all 945+ existing tests still pass alongside the new auth tests. If any existing tests fail due to the auth changes in Plan 01 (e.g., tests that hit api-public.ts routes), fix them by adding appropriate mock Authorization headers or mocking the Unkey verifyKey call.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/Handler/mission-control/apps/api" && npx vitest run src/services/unkey.test.ts src/middleware/auth.test.ts -x --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - unkey.test.ts has 10+ test cases covering AUTH-01 (key creation) and AUTH-02 (rotation, revocation)
    - TIER_RATE_LIMITS config validated with correct values
    - createApiKey verified to pass autoApply:true in ratelimits
    - rotateApiKey verified with default and custom expiration
    - revokeApiKey verified to call deleteKey
    - All new tests pass
    - Full regression suite (945+ tests) passes with no regressions
  </done>
</task>

</tasks>

<verification>
1. `cd mission-control/apps/api && npx vitest run src/middleware/auth.test.ts src/services/unkey.test.ts -x --reporter=verbose` -- all new tests pass
2. `cd mission-control && npm run test` -- full 945+ test suite passes (no regression)
3. `grep -rn "autoApply" mission-control/apps/api/src/services/unkey.test.ts` -- verifies autoApply:true is tested
4. `grep -rn "X-RateLimit" mission-control/apps/api/src/middleware/auth.test.ts` -- verifies header tests exist
5. `grep -rn "503\|service.*unavailable" mission-control/apps/api/src/middleware/auth.test.ts` -- verifies SDK error test exists
</verification>

<success_criteria>
- Shared Unkey mock utility provides configurable responses for all SDK operations
- Auth middleware tests cover: valid key, invalid key, rate limited, disabled, expired, free tier, SDK error
- Rate limit header tests verify headers present on success and error responses, absent on free tier
- Key management tests cover: creation with correct params + autoApply, rotation with default/custom expiration, revocation
- All new tests pass
- All existing 945+ tests pass (zero regression)
- AUTH-01, AUTH-02, AUTH-03, AUTH-04 each have at least one test proving the behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-access-control/01-02-SUMMARY.md`
</output>
